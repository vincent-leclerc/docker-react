language: generic

sudo: required
services:
    - docker

before_install:
    - docker build -t vincentleclerc/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true vincentleclerc/docker npm run test