language: generic

sudo: required
services:
    - docker

before_install:
    - docker build -t vincentleclerc/docker-react -f Dockerfile.dev .

script:
    - docker run vincentleclerc/docker npm run test -- --coverage